[variables]
NIXPACKS_NODEJS_VERSION = "18"

# top-level providers array (correct place)
providers = ["node"]

[phases.install]
cmd = "npm ci --only=production"

[phases.build]
cmd = "echo 'Backend build complete'"

[phases.start]
cmd = "bash start.sh"
