[variables]
NIXPACKS_NODEJS_VERSION = "18"
providers = "node"   # string, not an array

[phases.install]
cmd = "npm ci --only=production"

[phases.build]
cmd = "echo 'Backend build complete'"

[phases.start]
cmd = "bash start.sh"
