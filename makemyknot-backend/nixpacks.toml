[variables]
NIXPACKS_NODEJS_VERSION = "18"

# Top-level providers array (required)
providers = ["node"]

[phases.install]
cmd = "npm ci --only=production"

[phases.build]
cmd = "echo 'Backend build complete'"

[phases.start]
cmd = "bash start.sh"
